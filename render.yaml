services:
  - type: web
    name: flux-pipeline
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: pipeline-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
